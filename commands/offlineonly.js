module.exports = {
    name: "testxd123",
    cooldown: 3000,
    description: "Make the bot only type in when channel is offline",
    permission: 2,
    level: 3,
    execute: async(message, args) => {
        return {
            text: `xd`
        }
    }
}