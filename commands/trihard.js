module.exports = {
    execute: async (message, args) => {
        if (message.toLowerCase() === `forsen`) {
            return {
                text: `yo`
            }
        }
        
    },
};
